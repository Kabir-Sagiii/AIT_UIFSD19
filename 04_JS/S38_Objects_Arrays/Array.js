var info = [10, "AIT", true, null, { name: "Virat", city: "hyd" }];

//Literal Notation

//Property called length

// var numberOfElements = info.length;
// console.log(numberOfElements);

var element = info[10];
console.log(element);
